<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mytijian.mediator.order.DAO.OrderLogMapper">

	<resultMap id="OrderLogMap" type="com.mytijian.mediator.order.dto.OrderSyncLog">
		<id property="id" column="id"/>
		<result property="orderId" column="order_id" />
		<result property="errorCode" column="result_code" />
		<result property="errorText" column="result_desc" />
		<result property="operator" column="operator" />
		<result property="createTime" column="create_time" />
		<result property="updateTime" column="update_time"/>
		<result property="hospitalId" column="hospital_id"/>
	</resultMap>
	
	<sql id="columns">
		id,
		order_id,
		result_code,
		result_desc,
		operator,
		create_time,
		update_time,
		hospital_id
	</sql>
	
	<insert id = "insertOrderLog" parameterType = "com.mytijian.mediator.api.model.OrderSyncLog">
		<selectKey keyProperty="id" resultType="int" order="BEFORE">
			select nextval('tb_order_log_seq')
		</selectKey>
		insert into tb_order_log (<include refid="columns"/>)
		values (#{id},
				#{orderId},
				#{errorCode},
				#{errorText},
				#{operator},
				NOW(),
				NOW(),
				#{hospitalId})
	</insert>
	
	<select id = "getById" resultMap = "OrderLogMap">
		select * from tb_order_log where id = #{id}
	</select>
	
	<delete id="delete" parameterType="Integer">
		delete from tb_order_log where id = #{id}
	</delete>
</mapper>